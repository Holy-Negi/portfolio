---
title: Portfolio
description: 有機化学の研究現場から、バックエンド／化学DX領域へ
---

# 【氏名】

北海道大学 大学院生命科学院 修士1年 / 有機化学・ケミカルバイオロジー

有機合成の研究現場にいる人間として、そこにある非効率を自分で書いたコードで解決することに関心があります。
バックエンド・インフラ領域、および化学業界のIT／DX領域を志望しています。

- GitHub: [github.com/【ユーザー名】](https://github.com/【ユーザー名】)
- Contact: 【メールアドレス】

---

## Skills

すべて研究のかたわらでの独学・自作です。到達度を正確に記載しています。

**主に扱えるもの**

- **Python / FastAPI** — REST APIの設計・実装。Pydanticによる型定義とバリデーション
- **SQLAlchemy / Alembic** — モデル定義からマイグレーション運用まで
- **PostgreSQL / MySQL** — スキーマ設計から自分で行い運用。多対多を中間テーブルで表現
- **RDKit / PubChemPy** — 物性値算出、構造式・反応式のSVG描画、InChIKeyによる同一性判定
- **React 19 / Vite / Material-UI** — 状態管理を含む画面実装、APIとの連携
- **LaTeX** — 数式・化学構造を含む文書の組版

**基礎レベル**

- **Linux** — ローカルサーバの構築・運用経験（業務レベルの管理経験はなし）
- **PowerShell** — ファイル処理の自動化に使用した経験
- **Git / GitHub** — ブランチ運用を含むバージョン管理

---

## Projects

### 1. 電子実験ノート（ELN）— 有機合成向けWebアプリケーション

**FastAPI / SQLAlchemy / Alembic / PostgreSQL / RDKit / React 19** — [リポジトリ](https://github.com/【ユーザー名】/【リポジトリ名】)

紙の実験ノートを置き換えるため、バックエンドからフロントエンドまで一人で設計・実装したWebアプリケーション。

```
SMILES入力 → RDKitで物性計算 → PostgreSQL保存 → FastAPI提供 → React表示
```

【スクリーンショット：反応スキーム描画 or 当量表】

**解決した課題**

紙のノートは記録には足りるが、(1) 過去の類似実験を検索できない、(2) 反応のたびに当量を電卓で計算し直す、(3) 物性値を毎回調べ直す、という損失が慢性化していた。

**注力した機能（化学ドメイン固有の実装）**

| 機能 | 実装内容 |
|---|---|
| 当量表の自動計算 | スケール入力から必要物質量・質量・体積を算出。`equiv = 1.00` を律速試薬とみなす規約で運用し、密度を保持して液体は体積へ換算 |
| 反応スキーム描画 | 成分を role で3ブロックに仕分け → 反応SMILESを組立て → `ReactionFromSmarts(..., useSmiles=True)` → `DrawReaction` でSVG生成。重原子数に応じ描画幅を可変化 |
| InChIKeyによる重複検知 | InChIKeyに一意制約を設定し、`get_or_create_compound` で既存判定してから挿入。`/resolve` でPubChemPy経由の化合物名→SMILES変換にも対応 |

**データ設計**

`compounds` / `reactions` / `reaction_components` の3テーブル構成。
化合物と反応は多対多であり、1テーブルに詰め込むと同一化合物の情報が重複して不整合を招く。中間テーブルを挟み、**役割・当量・収率という「関係の属性」をそこに持たせる**ことで、化合物マスタを再利用可能にした。

**技術選定の理由**

- FastAPI — 化学系ライブラリがPythonに集中しており、RDKitと同一言語で完結できる。Pydanticの型定義がそのままAPIドキュメントになる点も評価
- PostgreSQL — 【実際の選定理由に差し替え】
- Alembic — スキーマ変更をコードとして管理。開発途中の温度・時間カラム追加を履歴に残せた

**主なエンドポイント**

| Method | Path | 概要 |
|---|---|---|
| GET | `/depict` | SMILES → 構造式SVG |
| GET | `/resolve` | 化合物名 → SMILES（PubChemPy） |
| GET | `/reactions/{id}/equivalents` | 当量表の算出 |
| GET | `/reactions/{id}/scheme` | 反応スキームSVGの生成 |

**Roadmap**

- 進行中：反応の温度・時間の登録（バックエンド実装済み／フロント入力欄を実装中）
- 部分構造検索（RDKit substructure search）
- 実験項の自動生成（`To a solution of X (1.2 equiv) ...` を反応＋当量表から生成）
- 試薬の在庫量計算 / 当量表からの収率登録

> 掲載データはすべてダミーです。研究室の実データは含まれません。

---

### 2. 実験データ管理データベース

**MySQL / SQL**

ELN以前に構築した、実験データ管理DB。ELNのデータ設計はこの経験を土台にしている。

- 【テーブル構成を1行で】
- 【設計上の教訓を1行で。ELNの3テーブル構成に繋がった話にできると強い】
- 結果：【定量的な改善を1行で】

---

### 3. SMILESからの分子物性 一括算出スクリプト

**Python / RDKit**

RDKitに触れた最初の題材。SMILESのリストから分子量・logP等を一括算出する。
この処理は現在、ELNの化合物登録機能として組み込まれている。

- 【つまずいた点と解決を1行で】
- 結果：【定量的な改善を1行で】

---

## Research

タンパク質中の**トリプトファン残基のみを光触媒的に選択的修飾する手法**の開発。
細胞内のタンパク質間相互作用を解析する近接依存性標識への応用を目指す基盤研究。
解析はLCMSが主体で、「測定データをどう構造化し蓄積・検索するか」という問題意識はこの環境から生まれた。

---

## Interests

- バックエンド／インフラ領域のシステム開発
- 化学・材料分野のDX、マテリアルズ・インフォマティクス
- 研究開発現場を支える情報基盤の構築

化学の現場を実際に知っていること、その現場の課題を自分でコードにできること。この2点を併せ持つ立ち位置から価値を出したい。

---

<sub>最終更新：2026年8月</sub>
